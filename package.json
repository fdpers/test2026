{
  "name": "sanshi-academy",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-academy.mjs",
    "check:release": "node scripts/check-release.mjs",
    "test:build": "node --test sanshi/tests/ai-build.test.mjs",
    "test": "npm run check:release && npm run test:build"
  }
}
